import {doStuff as stuff} from "./esNextModules";

// lo llamo implicitamente
stuff("llamada desde el Modulo 2");

// default lo importa y lo pone por defecto
// si el que importas tiene un default
export default class A {

}